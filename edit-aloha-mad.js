requirejs.config({
  xhtml: true,
  baseUrl: "Aloha-Editor/src",
  paths: {
    jquery: "../../jquery-2.1.1"
  }
});

define('edit-autogenerated', function(){
  var head = document.getElementsByTagName('head')[0];
  var autogen;
  if(head) {
    autogen = document.createComment("autogenerated tags follows");
    head.appendChild(autogen);
  }
  return autogen;
});

requirejs(['edit-autogenerated'], function(){  
  requirejs(['../../edit/main'], function(aloha){
  });
});

//var $ = jQuery;
//requirejs.load = function(context, moduleName, url){
//    console.log("Trying to load " + moduleName + " from " + context + " at location " + url);
//    $.getScript(url);
//}
/*
Aloha = {
    settings: {
        bundles: {
            edit: "../../../plugins"
        },
        sidebar: {
            disabled: true
        },
        requireConfig: {
            paths: {
                jquery: "../../../jquery-1.11.1"
            }
        }
    }
};
*/
